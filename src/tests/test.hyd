
func test() -> void {
    char[] hello = {'H', 'e', 'l', 'l', 'o', ' '}; // initialization of char array
    char[] world = {'W', 'o', 'r', 'l', 'd', '!'}; // initialization of char array

    forEach (char c in hello) { // forEach loop to iterate over each element in the hello array
        if (c is in world) { // if check to see if the character on current iteration is an element in the world array
            skip; // skip print if true
        }

        stdout("%c", c); print c
    }
    stdout(); // print new line
}

func main() -> void {
    stdout(test());
}